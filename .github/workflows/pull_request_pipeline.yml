name: PR pipeline

on:
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    name: pull_request_pipeline
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make
    - name: unittests
      run: make unittests
    - name: perftests
      run: make perftests
